<h1><%= @artist.name %></h1>
<h2>Genre - <%= @artist.genre %></h2>

<%= link_to 'Edit', edit_artist_path(@artist) %>
<%= link_to 'Delete', @artist, method: :delete %></h3>
<%= link_to 'Songs', artist_songs_path(@artist) %>

<p><%= link_to 'Back', billbs_path %></p>

